extends layout

block content
	body
		main.top-content
			header
				h1 Eyðublað
		.wrapper
			if friends
				form(method="post")
					select(class="menu" name="friendsval")
						each u in friends
							option(value=u.friend ) #{u.friend}
						input(type="submit" class="takki" )
